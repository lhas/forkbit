development:
  sessions:
    default:
      database: forkbit_development
      hosts:
        - localhost:27017
      options:
  options:

test:
  sessions:
    default:
      database: forkbit_test
      hosts:
        - localhost:27017
      options:
  options:

production:
  sessions:
    default:
      url: <%= ENV['MONGOHQ_URL'] %>
      options:
        consistency: :strong
        max_retries: 1
        retry_interval: 0 